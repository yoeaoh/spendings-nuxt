import type { ICategory } from '~/interfaces/category.interface';

export const DEFAULT_CATEGORY: ICategory = {
    id: '__0',
    name: 'Без категории',
    sum: 0,
    subCategories: [],
};
export const DEFAULT_CATEGORY_ID: string = '__0';
